<!DOCTYPE html>
<html>
<head>
  <title>BadJokesApp</title>
  <%= stylesheet_link_tag    "application", :media => "all" %>
  <%= javascript_include_tag "application" %>
  <%= csrf_meta_tags %>
</head>
<body>

<% if current_user? %>

<%= form_tag "sessions/#{@current_user.id}", :method => "delete" %>
<h2 align="right"> <%= button_to "Logout", logout_path %> </h2>

<% else %>

<h2 align="right"><%= button_to "Login", login_path %></h2>

<% end %>

<%= yield %>

</body>
</html>
